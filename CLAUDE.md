# CLAUDE.md — Second Year Proposal

## Project

**Title:** Local Control and Metropolitan Inefficiency: The Interaction between Zoning and Transportation Investment

**Author:** Ke Jiang | PhD student, Economics | Advisor: WK

**Research question:** How does fragmented local zoning control distort metropolitan transportation investment, and what is the welfare cost of this coordination failure? The central mechanism: municipalities choose zoning to maximize incumbent-resident welfare, suppressing density near high-accessibility locations. The regional transportation authority responds to this constrained spatial pattern, finding transit investment less attractive and road investment relatively more so. The result is a path-dependent car-dependent equilibrium even when coordinated zoning + transit would dominate.

**Why transit is special:** Unlike roads (extensive-margin, benefits diffuse across space), transit is intensive-margin infrastructure: its return depends on the density of residents and jobs *at* station-area locations. Zoning restrictions directly suppress this density, lowering transit ridership and the marginal return to transit investment.

---

## Model Architecture

**Type:** quantitative spatial equilibrium model.

**Key agents:**
- **Municaplity** in each municipality `g`: chooses optimal zoning regulations(both resitential and commercial landuse) to maximize the local incumbents' welfare, in terms of congestion disutility.
- **Households** `ω`: Cobb-Douglas preferences over consumption and housing; idiosyncratic Fréchet taste shocks `υ_{ωij}`; choose residence `i`, workplace `j`.
-  **Firms** At each location j, competitive firms produce a freely tradable good using labor and commercial land, following CoD production function, with A_{j} be location-specific productivity.
- **Transportation authority**: maximizes aggregate household welfare subject to budget constraint `K`.

**Key structure:**
- Genuine network `(S,E)`: transportation investment `I_{kℓ}` is chosen per **edge** `(k,ℓ)∈E`, not per residence-workplace pair. Each OD pair `(i,j)` has an exogenously fixed route `r_{ij}⊆E` (GIS shortest path / observed transit line); commuting cost accumulates multiplicatively over the route: `τ_ij = ∏_{(k,ℓ)∈r_ij} d_{kℓ}(I_{kℓ}, n_{kℓ})`, with edge-level friction `d_{kℓ}=δ^τ_{kℓ} n_{kℓ}^ρ/I_{kℓ}^γ`. Edge traffic `n_{kℓ} = Σ_{(i,j):(k,ℓ)∈r_ij} Nπ_ij`. Endogenous multi-route choice (Allen-Arkolakis style) is a deferred extension, not built.
- Budget constraint `Σ_{(k,ℓ)∈E} c_{kℓ} I_{kℓ} ≤ K` (renamed 2026-07-21 from `δ_{kℓ}` — was easily confused with the travel-time parameters `δ⁰_{kℓ}, δ¹_{kℓ}`; `c_{kℓ}` is the per-unit construction/investment cost).
- Household budget: `c_{ij} + h_{ij} q_i ≤ w_j` (revised 2026-07-20 — congestion `φ(N_i,L_i)` moved into the amenity, see Notation Registry `B_i`/`φ` entries).
- Land is owned by absentee landlords who collect all rent and consume it outside the metro; no feedback into household budgets or municipal welfare (added 2026-07-20).

---

## Notation Registry

Do not introduce new symbols for existing concepts. All sessions must use this registry.

| Symbol | Meaning |
|--------|---------|
| `g ∈ G` | Municipality |
| `i, o` | Location (residence origin) |
| `j` | Workplace location |
| `m ∈ {R, T}` | Mode: R = road, T = transit |
| `h_{0,g}` | Zoning capacity chosen at t=0 by municipality g |
| `q_i` | Housing price at location i. Rent (`q_i^R H_i^R + q_i^F H_i^F`) accrues to absentee landlords who consume outside the metro (added 2026-07-20) — no feedback into household budgets or municipal welfare. |
| `w_j` | Wage at workplace j |
| `N_i` | Population at location i |
| `I_{kℓ}` | Transportation investment on network edge (k,ℓ)∈E (edge-level, not per OD pair) |
| `r_{ij}`, `n_{kℓ}` | Exogenous fixed route from i to j (⊆E); edge-level traffic flow aggregating all OD pairs whose route uses that edge |
| `K` | Total transportation budget |
| `d_{ij}(I,N)` | Commuting cost (generic) spatial friction; 
| `φ(N_i,L_i) = η N_i^a L_i^b` | Congestion/density disutility, Cobb-Douglas form: both local residents and local employment cause disutility. `η>0` overall scale; `a,b>0` relative weights; `a>1` needed for own-argument convexity (interior FOC). Generates a complementarity (`∂²φ/∂N_i∂L_i>0`) absent from an additive parametrization. **Revised 2026-07-20 (advisor's July_17th comment):** no longer subtracted from income; enters as a markdown on the amenity, `B_i(N_i,L_i)=B̄_i/φ(N_i,L_i)` — see `B_i` entry below. Functional form and convexity condition unchanged. |

| `ε` | Fréchet shape parameter (idiosyncratic taste shocks) |
| `α` | Cobb-Douglas consumption share |
| `B_i(N_i,L_i) = B̄_i/φ(N_i,L_i)` | Effective residential amenity at `i` (revised 2026-07-20, supersedes plain exogenous `B_i`): exogenous location level `B̄_i`, marked down by the congestion term `φ(N_i,L_i)`. Folding congestion into the amenity rather than income makes the partial `∂Ū_i/∂φ_i` exact and closed-form (`=-Ū_i/φ_i`, vs. a signed-only partial before) and removes the first-order log approximation previously needed in the gravity/quantification equations — the advisor's "more coherent to interpret and easier to calibrate" note. `B̄_i` (not `B_i`) is the object recovered as a residual in calibration, jointly with `T_i`, net of the `(a,b)` congestion terms identified from `N_i,L_i` regressors in the origin fixed effect. |
| `T_i, E_j` | Fréchet scale parameters at residence i and workplace j |
| `t_i ≥ 0` (defined via `t_i = q_i^F/q_i^R`, eq. `eq:arbitrage`) | **Primary municipal instrument, plain-ratio "zoning wedge" framing (simplified 2026-07-23, supersedes the 2026-07-11 iceberg-cost `(1+t_i^F)` framing — reverted directly in `model.tex` for tractability, "more convenient").** `t_i>1` is pro-residential zoning, `t_i<1` pro-commercial. Land is competitively allocated between residential and commercial use; if both uses are active, competition equalizes returns at the margin, giving `t_i=q_i^F/q_i^R`. Combined with the residential rent-clearing condition (`eq:rent-clearing`) this pins down `H_i^R(t_i)` — strictly increasing, `dH̄_i^R/dt_i=H̄_i^R/t_i>0` (eq. `zoning-foc-tax`, re-derived 2026-07-23 under the simpler ratio; the old `(1+t_i^F)`-based version is gone). No developer or supply-side agent, no assumed cost schedule. `H_i^F` itself can still be read as a *quota* (cap on commercial floor space). Empirically recovered as `t_i = q_i^F/q_i^R` from rent data directly, no estimation — same Glaeser-Gyourko rent data as before. **Note:** the closed-form solution for `t_i` (formerly item 11, `θ_i^*`/`eq:theta-star-leading`, built on the iceberg-cost version) was removed from `model.tex` along with the iceberg-cost framing on 2026-07-23 and has not been re-derived under the new ratio; item 11 below is marked reverted, not resolved. |

---

## Open Model Questions

Track which issues from SB's feedback are resolved vs. pending. **Read `comments from SB.md` before editing any model section.**

| # | Issue | Status | Note |
|---|-------|--------|------|
| 1 | Road vs. transit structural distinction | **OPEN** | Need mode-specific `τ_{ijm}` with asymmetric density complementarity. Not yet in model.tex. SB: road = extensive margin, transit = intensive margin. Put this distinction apart first, just consider in general the transportation|
| 2 | Municipal objective ω interpretation | **OPEN** | Must frame as reduced-form political-economy weights, not welfare weights. Discipline via housing supply elasticity calibration. Not yet in model.tex. |
| 3 | Nested-choice relocation friction | DEFERRED | Optional extension. User noted "think about later." |
| 4 | Timing: simultaneous vs. sequential | **RESOLVED** | 
Simultaneously decided, both municipality and transportation investment come in the form of Nash Equilibrium
| 5 | Quantification strategy | RESOLVED (plan) | Match equilibrium objects: commuting flows, mode shares, rents, employment. Data: NTD, LEHD/LODES, ACS, Baum-Snow & Han (2024) elasticity. Do NOT estimate all primitives directly. |
|6| Scale of decision | **RESOLVED (2026-07-23), corrected twice more the same day — see below for full history** | Added `\subsection{Scale of the Zoning Decision}` to `model.tex`, right before `\subsection{Social Planner}`: a **Metropolitan Zoning Authority** replaces the $|G|$ municipalities with one zoning agent choosing $\{t_i\}_{i\in S}$ (all locations), still in Nash with the (unchanged) transportation authority. **Current version (final):** the authority's objective was changed from the incumbent-style `Σ_{i\in S}N_i\bar U_i` to the *same population-wide* `NΓ(1-1/ε)Φ^{1/ε}` used by the transportation authority and social planner — `Ū_i` (residence-held-fixed, incumbent) is now specific to the genuinely local municipal government (item 18); a metro-wide authority doesn't obviously inherit that "protect a fixed set of current residents" story. With matching objective and scope, the authority's zoning FOC is *exactly* `zoning-foc-planner` (removing the edge/route/budget terms it doesn't have, since it doesn't choose `I`, changes nothing about the `H̄_i^R`-FOC, which never touched those terms) — so **agent identity is welfare-irrelevant for the zoning margin**, now verified rather than assumed. The remaining, economically meaningful comparison is local/incumbent (municipal, `Σ N_i\bar U_i`) vs.\ regional/aggregate (metro authority and planner, `Ū`) — evaluated quantitatively via DE-vs-SP in §Welfare Comparison. `zoning_and_transportation_governance_us.md` remains the reference for real-world institutional detail, not yet mapped further.
| 7 | Municipal instrument: quantity vs. price/quota | **RESOLVED (revised 2026-07-11, simplified again 2026-07-23)** | SB (via `july_1st_comment.md`, 2026-07-01): municipality should not directly control land-supply quantity; think price/quota, subject to data availability. Resolved *without* adding a developer, endogenous supply side, or assumed cost schedule — total land `H̄_i` stays fixed. **Revised 2026-07-11** per the advisor's "July_6th comment": read the instrument as an iceberg cost, `q_i^R=q_i^F/(1+t_i^F)`, rather than an additive tax. **Reverted 2026-07-23:** simplified back to the plain ratio `t_i=q_i^F/q_i^R` (notation registry entry `t_i`) for tractability — the iceberg-cost `(1+t_i^F)` framing and its associated closed-form derivation (item 11) were removed from `model.tex`. Monotonicity `H_i^R(t_i)` re-derived under the simpler ratio (`zoning-foc-tax`, `dH̄_i^R/dt_i=H̄_i^R/t_i>0`); qualitative comparative statics unchanged. `empirical facts.tex` and `main.tex`'s model subsection remain un-synced.
| 9 | `I_{jk}` / network structure ambiguity | **RESOLVED (2026-07-11)** | Advisor's July_6th comment flagged that `model.tex` used `I_ij` (OD-pair-indexed) in the Environment section but `I_jk` (link-indexed) in the Transportation Authority section, with no actual network topology connecting the two. Read Fajgelbaum, Gaubert, Gorton, Morales & Schaal (2023/24, NBER w31438) and Bordeu (2025, "Commuting Infrastructure in Fragmented Cities") — confirmed the advisor's "lanes as investment" description actually refers to Fajgelbaum & Schaal (2020), already cited in model.tex, not w31438 (which has no continuous per-link investment variable — CHSR's design choice is discrete station placement along an exogenous route). Resolved by defining a genuine network `(S,E)` with edge-level investment `I_kℓ` and an exogenously fixed route `r_ij` per OD pair (GIS shortest path / observed transit line), following Bordeu's edge-level parametrization but without her endogenous multi-route choice (deferred, flagged as future extension in model.tex). |
| 10 | Investment spillovers | **RESOLVED (2026-07-11)** | Advisor's July_6th comment: commuting cost between `j` and `k` was only affected by `I_jk`, not by investment elsewhere on the path. Resolved as a corollary of Item 9's network structure: since `τ_ij = ∏_{(k,ℓ)∈r_ij} d_kℓ`, the transportation authority's FOC (`transit-foc` in model.tex) sums the marginal benefit of edge `(k,ℓ)`'s investment over *every* OD pair whose route uses that edge — spillovers fall out of the route-sum algebra, not an ad hoc added term. Full endogenous-routing spillovers (Allen-Arkolakis matrix-inverse propagation, as in Bordeu) remain a deferred extension.
| 11 | Closed-form solution to municipal zoning problem | **REVERTED (2026-07-23) — was briefly RESOLVED same day, then removed.** The prior 2026-07-16 entry described a derivation that was never actually written to `model.tex` at all (checked every past commit via `git log -p -S`). A real derivation was built the same session (small-open-location approximation, log-linear "hat algebra" system, closed form `θ_i^*`) — but it was built on the iceberg-cost `(1+t_i^F)` framing, which was itself reverted to the plain ratio `t_i=q_i^F/q_i^R` later the same day (see notation registry, `t_i`) for tractability. The closed-form derivation was removed from `model.tex` along with it and has not been rebuilt under the simpler ratio. Status: genuinely open again, not resolved. |
---
| 8 | Zoning restriction: trade-off btw residential and commerical|**Open**| The way the municipal set zoning in the model is merely choosing between residential and commerical, wheras the total housing supply is fixed and fully utilized, for now the less residential use equals to more commerical land use, but in reality is not the case, with so many unutilized land.
| 12 | Congestion disutility placement (`φ` into `B_i`) | **RESOLVED (2026-07-20)** | Advisor's July_17th comment (`july_1st_comment.md`): putting `φ(N,L)` into `B_i` is "more coherent to interpret and potentially easier to calibrate." Rederived: `B_i(N_i,L_i)=B̄_i/φ(N_i,L_i)` replaces the income-side `w_j-φ(N_i,L_i)`; budget is now `Q_ij+q_i^Rh_ij^R≤w_j`. Makes `∂Ū_i/∂φ_i=-Ū_i/φ_i` and `∂Ū_i/∂w_i=π_{ii\|i}Ū_i/w_i` exact (no approximation), and removes the first-order log approximation from the commented Quantification gravity equation — `(a,b)` now identified directly from `N_i,L_i` regressors in the origin FE. §Households, §Municipal Zoning (Channels paragraph, `zoning-foc-explicit`), and the commented Quantification block in `model.tex` updated; see `B_i`/`φ` entries in the Notation Registry. |
| 13 | Landlord / land-rent destination | **RESOLVED (2026-07-20)** | Advisor's July_17th comment asked where land rent goes. Resolved with an absentee-landlord assumption: landlords collect `q_i^RH_i^R+q_i^FH_i^F` and consume it outside the metro, no feedback into household budgets or municipal welfare. Stated in §Environment, `model.tex`. The alternative (rebate to local residents) is left for the land-supplier extension the advisor flagged as a later step. |
| 14 | Bordeu (2025/26) land-value vs. `Ū` equivalence | **RESOLVED (2026-07-20, noted contrast, no model change)** | Advisor's July_17th comment: Bordeu models the local government's objective as maximizing land value, sometimes equivalent to maximizing `Ū`; be aware of this. Under this paper's absentee-landlord assumption (item 13), the equivalence does *not* hold — it requires residents to also be the landowners capturing their own parcel's rent. Documented as a footnote in §Municipal Zoning, `model.tex`, citing `Bordeu2026-df`. |
| 15 | Why zoning favors transportation investment (job access, agglomeration) | **RESOLVED (formalized 2026-07-21, supersedes the earlier "no new model object" note)** | Advisor's July_17th comment listed two channels: better job access, productivity agglomeration. The original resolution claimed both were already captured via the amenity/wage channels — this was too hasty: the wage channel was a pure labor-market-clearing quantity effect under exogenous `A_j`, with no actual productivity externality. **Revised**: added a genuine Marshallian agglomeration externality, `A_j=Ā_jL_j^ζ` (`eq:agglomeration`, §Firms), `ζ∈(0,1-γ)` a new stability condition (parallel to `a>1` for `φ`). Firms remain atomistic (take `A_j` as given), so the firm's FOC is unchanged in form; substituting into labor demand gives an explicit solved form `L_j=[γĀ_j(H̄_j^F)^{1-γ}/w_j]^{1/(1-γ-ζ)}` (`eq:labor-demand-agglom`). Downstream: (i) §Municipal Zoning's wage-loss term (5) in `zoning-foc-explicit` now compounds a quantity effect with a genuine productivity loss; (ii) §Transportation Authority's wage-feedback term `dln w_j/dI_kl` in `eq:V-total-derivative` now has an explicit agglomeration channel (investment → job access → `L_j` → `A_j` → `w_j`), a spillover to every OD pair with workplace `j`, distinct from the route-based spillover — new paragraph "The productivity-agglomeration channel." `A_j` is now endogenous everywhere; `Ā_j` (fundamental) replaces it as the residual recovered in §Data and Calibration, net of `ζ ln L_j`, same logic as `B̄_i` net of `(a,b)`. `ζ` set externally from existing urban-econ agglomeration-elasticity estimates — a dedicated citation still needs to be added to `reference.bib`. |
| 16 | Endogenize total budget `K` | DEFERRED | Advisor's July_17th comment flags `K` as a candidate for endogenizing in a future extension. Same status class as item 3 (nested-choice relocation friction) — not built. |
| 18 | `Ū_i` interpretation: is it really "expected utility of a resident of `i`"? | **SUPERSEDED (2026-07-27) — see item 21** | Working out whether the municipal objective `W^g=Σ N_iŪ_i` should instead be `N^g·Ū` (aggregate `Ū=Γ(1-1/ε)Φ^{1/ε}`), a Fréchet/GEV max-stability argument shows the expected utility of a household *freely choosing* its residence from the whole metro is the **same constant `Ū` at every location** — a compensating-shock/selection result (reducing residence choice to a logit over meta-alternatives with systematic values `ln Φ_i`, then applying McFadden's classical invariance). Switching the objective to `N^g·Ū` was tried and rejected: since `Ū` is fixed for a small (negligible, per item 11's small-open-location logic) municipality, the FOC `Ū\,dN^g/dH̄_i^R+N^g\,dŪ/dH̄_i^R=0` has no interior solution — the municipality would always want to zone everything residential (attracting residents is free in expected-utility terms), directly contradicting the paper's incumbent-protection mechanism. **Resolution:** keep `Ū_i=Γ(1-1/ε)Φ_i^{1/ε}` (no equation changes), but state explicitly what it *is* — the expected utility of a household with residence `i` held **exogenously fixed** (an incumbent, not conditional on freely selecting `i`), optimizing only over workplace `j`. This is the economically correct object for a municipal government representing incumbents specifically (per the paper's own framing), as distinct from `Ū`, which correctly represents the whole population (including the free residence margin) and is what the transportation authority and social planner use. New paragraph "Two notions of expected utility" added to §Households (right after `eq:pi-pop`, `eq:Ubar-i`); §Municipal Zoning's `W^g` definition now cites it and footnotes the rejected `N^g·Ū` alternative. |
| 19 | Municipal problem not stated subject to equilibrium (parallel to item 17 for the TA) | **RESOLVED (2026-07-23)** | `zoning-foc`/`zoning-foc-explicit` were derived by implicit differentiation, treating `∂N_i/∂H̄_i^R` etc. as unspecified GE objects — the same shortcut item 17 already ruled out for the transportation authority. Rebuilt as a full Lagrangian, `eq:muni-lagrangian`, reusing `λ_ij` (household optimality), `η_i^q` (rent clearing), `η_j^w` (labor clearing) from `eq:TA-lagrangian` and `ν_i` (land identity) from `eq:SP-problem`, plus two genuinely new multipliers `σ_i,ρ_i` on population/employment identities (needed because `Ū_i` multiplies `N_i` *outside* `Φ_i` in the municipal objective, unlike the TA/planner's `Φ`-only objective) and `χ_i` on a pulled-out congestion identity `φ_i=ηN_i^aL_i^b` (mirrors how `d_kl` is pulled out in `eq:TA-lagrangian`, so `N_i,L_i` avoid appearing inside every `λ_ij` bracket). `N_i,L_i` and the `H̄_i^R`-vs-`H̄_i^F` comparison (via `ν_i`) get clean closed-form FOCs (`eq:muni-NL-focs`, `eq:muni-land-foc`); `φ_i,q_i^R,w_i` each enter `Ṽ_ij` directly and so need "closing the system" the same way `τ_ij,d_kl` do for the TA — new `λ`-weighted market-access objects `Λ_i^φ,Λ_i^q,Λ_i^w` (the last summing over the *whole* metro, not just `S^g`), not expanded symbol-by-symbol, same convention as `Λ_kl`. Verified the small-open-location approximation (item 11) is recoverable as the named special case that reproduces `zoning-foc-explicit`'s five channels exactly — the existing Channels paragraph is unchanged, now framed as *that* special case rather than the primary derivation. §Municipal Zoning, `model.tex`, between `zoning-foc` and the (unchanged) Channels paragraph. |
| 17 | Transportation Authority FOC: stale elasticity, missing GE feedback, problem not stated subject to equilibrium | **RESOLVED (2026-07-21, corrected same day — see below)** | `transit-foc` predated the network/congestion cost structure in §Transportation and used a leftover single-route power-law elasticity; rederived direct edge elasticity (`eq:d-elasticity`) and the chain rule through nested route choice using `π_ij^{kl}` (`eq:edge-intensity`). **First revision (superseded)**: stated the problem "subject to equilibrium" only in prose, then computed `dΦ/dI_kl` by direct chain-rule differentiation and waved away wage/rent/population feedback as "negligible to leading order" — flagged by the user as methodologically wrong, since this isn't how the literature the model is built on actually does it. **Second revision (current)**: checked Olivia Bordeu's actual paper (`Bordeu2026-df`, fetched and read directly, §2.1.4 + Appendix A.3/A.5) — her local government's problem enters *every* equilibrium condition into a Lagrangian with its own multiplier (`λ_ij` travel demand, `ϕ_kl` traffic, `η_Ri,η_Fj` land/wage clearing), never approximates them away, and computes them as a linear system given the current equilibrium (Appendix A.5). Redid the TA's problem the same way: full Lagrangian (`eq:TA-lagrangian`) with multipliers `λ_ij` (OD choice), `φ_kl` (edge traffic), `η_i^q` (rent clearing), `η_j^w` (labor clearing) on top of `μ` (budget, which this model keeps as a genuine hard constraint, unlike Bordeu's unconstrained net-benefit objective). The `I_kl`-FOC and `n_kl`-FOC share the same `λ`-weighted market-access sum `Λ_kl`; their ratio reproduces Bordeu's eq. (18) structure almost exactly (`transit-foc`), and substituting the budget constraint gives the same closed-form budget-share rule as before but with `Λ_kl` (properly `λ`-weighted) replacing the naive, uniformly-weighted `M_kl`. The old "leading-order" shortcut is now precisely identified as the special case `λ_ij≡ε·Γ(1-1/ε)Φ^{1/ε-1}` for every OD pair — a named, checkable assumption, not an unexplained approximation. Agglomeration channel (item 15) re-anchored to `η_j^w`'s own FOC. Resolution algorithm now explicitly follows Bordeu's Appendix A.5 three-step nested procedure (linear multiplier system → closed-form `I*` → resolve nonlinear equilibrium → iterate), deferred to quantification. Still does *not* resolve item 1 (road/transit structural distinction). §Transportation Authority, `model.tex`. |
| 20 | Municipal objective not "at the same level" as TA/planner — population weighting | **RESOLVED (2026-07-27)** | Advisor's July_27th comment (`july_1st_comment.md`, "Optimization of Municipality"): to make the maximization targets comparable across agents, local municipality, metropolitan zoning authority, and TA should all maximize the *average* indirect utility `Ū`, with the local analogue being `Ū_i` — not `N_i·Ū_i` / `N·Ū`. Distinct from item 18 (which utility object, `Ū_i` vs.\ `Ū`, is used) — this is about whether population *weights* that object. For the TA/metro authority/planner this is FOC-neutral (`N` is a fixed constant, not a choice variable, so the argmax is unaffected) — their stated objectives were rewritten from `N·Γ(1-1/ε)Φ^{1/ε}` to `Γ(1-1/ε)Φ^{1/ε}` in `eq:TA-problem`, `eq:TA-lagrangian`, the Metropolitan Zoning Authority's objective, and `eq:SP-problem`, purely for "same-level" consistency; downstream `Λ`-based FOCs (`eq:I-optimal`, `zoning-foc-planner`) needed no edits since they are self-referential to their own (consistently rescaled) Lagrangian. For the municipality this is **not** FOC-neutral: `N_i` is itself a function of the zoning choice `H̄_i^R`, not a fixed weight, so `W^g` was changed from `Σ_{i∈S^g}N_i\bar U_i` to `Σ_{i∈S^g}\bar U_i`. Rederived the affected Lagrangian pieces by hand (verified against the pre-existing `χ_i=(N_i\bar U_i+εΛ_i^φ)/φ_i`-type formulas, reproducing the same differentiation method): `σ_i` loses its `-\bar U_i` term (now `σ_i=χ_ia φ_i/N_i`, identical in form to the planner's, item 19/`eq:SP-problem`); `χ_i,η_i^q`, and the analogous `η_i^w`-closing term lose their `N_i` factor; `zoning-foc-explicit` **loses channel (1) (in-mover) entirely** and collapses from five channels to four (rent, residential congestion, worker-congestion relief, local-wage loss) — the municipality no longer values attracting residents *per se*, only through the price/congestion channels they trigger, the same structural reason the planner's `zoning-foc-planner` already lacked a channel-(1) analogue. This closes most of the gap between the municipal and planner zoning conditions: both are now four-channel, no-in-mover conditions, differing only in `Ū_i` (local) vs.\ `Ū` (aggregate) and in `Λ_i^φ,Λ_i^q,Λ_i^w` being collapsible (municipal, small-open-location) vs.\ genuinely live (planner, spans all of `S`) — updated in the "Zoning" paragraph of §Social Planner and the closing `Remark` of §Welfare Comparison, which no longer cites "missing channel (1)" as a distinguishing feature between the two conditions. Whether this sharper parallel licenses a cleaner leading-order welfare-gap decomposition than Proposition~2's current form is flagged as an open follow-up, not attempted in this pass. §Municipal Zoning, §Transportation Authority, §Scale of the Zoning Decision, §Social Planner, §Welfare Comparison, `model.tex`. |
| 21 | Retire `Ū_i` — use `Ū` uniformly for municipal, metro-authority, and planner objectives | **RESOLVED (2026-07-27), supersedes item 18 — mechanism corrected same day, see note below** | User direction (self-directed follow-up the same day as item 20, in `model.tex` directly): set `Ū_i≡Ū` — the municipality's zoning objective now uses the same free-mobility expected utility `Ū=Γ(1-1/ε)Φ^{1/ε}` as the TA/metro authority/planner, not the residence-fixed incumbent `Ū_i=Γ(1-1/ε)Φ_i^{1/ε}` that item 18 established. Confirmed via clarifying question that this requires dropping the small-open-location approximation for the municipal case too — keeping it would set `∂Ū/∂H̄_i^R≈0` for a small municipality (population is fixed, so `Ū`'s aggregate partials, e.g. `∂Ū/∂q_i^R=-(1-α)(N_i/N)Ū/q_i^R` via `Φ_i/Φ=N_i/N`, wash out for negligible `N_i/N`) — a genuine degeneracy (zoning payoff-irrelevant to first order), not just a loss of closed form. Chose the exact treatment instead (no local approximation). **Consequence, worked out in full:** every zoning-setter in the model — municipality (`S^g`, Nash), metropolitan zoning authority (`S`, Nash), social planner (`S`, joint with `I_kl`) — now solves the *literal same* first-order condition, `zoning-foc-planner`; the only remaining differences are scope and Nash-vs-joint solution concept. `eq:muni-lagrangian`'s objective changed from `Φ_i^{1/ε}` to `Φ^{1/ε}`; the "Closing the system" `χ_i,η_i^q` formulas picked up the `N_i/N`-type factor via `Φ_i/Φ=N_i/N`, becoming identical to the planner's `eq:SP-etaq`. The old "Channels" paragraph and `zoning-foc-explicit` (the local 4-channel rent/congestion/wage-loss condition, item 20's result) are **retired** — replaced by a direct pointer to `zoning-foc-planner` plus a new remark explaining the retired small-open-location special case is now near-degenerate, precisely because `Ū` no longer varies by location the way `Ū_i` did. §Scale of the Zoning Decision and §Social Planner's "Zoning" paragraph updated to drop the now-moot `Ū` vs.\ `Ū_i` comparison. §Welfare Comparison's closing `Remark` rewritten: the DE-vs-SP gap is no longer "different objectives" (item 20's framing) but a standard strategic-externality story — municipalities satisfy `zoning-foc-planner` restricted to their own `S^g` only as a **Nash best response** (taking other zoning and investment as given), while the planner's `∂W/∂H̄_i^R|_{DE}` in Proposition~2 is the true simultaneous partial derivative accounting for how zoning at `i` moves `Φ` for everyone else. Explicitly does *not* attempt a closed-form Nash-vs-joint wedge — deferred to quantification, same convention as the `Λ`-system throughout. §Households ("Two notions of expected utility"), §Municipal Zoning, §Scale of the Zoning Decision, §Social Planner, §Welfare Comparison, `model.tex`. **Mechanism corrected (2026-07-27, later same day):** user pushback ("I still can't get the difference between a Nash BR and a planner welfare... I can't see it through what you have derived") exposed that the "Nash best response vs.\ joint optimization" framing above is not actually a valid mechanism — when every zoning-setter maximizes the *identical* scalar `Ū`, a Nash equilibrium of players each solving their own FOC taking others' *current* choices as given is mathematically the same system of equations as everyone's FOC holding simultaneously (a team game's Nash equilibrium coincides with the joint optimum of a shared objective), so "Nash vs.\ joint" predicts *no* gap under a literally shared objective, not a gap — the opposite of what was claimed. The actual mechanism, found by inspecting `eq:muni-lagrangian` directly: `χ_i,σ_i,ρ_i,η_i^q` are summed over `i∈S^g` only, while `λ_{i'j'},η_j^w` are summed over the *whole* metro `S` (they have to be, since a municipality's own zoning move reprices every OD pair through the shared `Φ`). This asymmetry means `λ_{ij}`'s own first-order condition — which references `σ_i,ρ_j,η_i^q` — is missing whichever of those terms belongs to a location outside `S^g`, for *any* OD pair touching such a location, because that location's population/employment/rent identity was never written into the municipality's problem at all (not assumed fixed — simply absent as an equation). This contaminates `Λ_i^q,Λ_i^w` even for the municipality's *own* locations `i∈S^g`, since those sums run over every workplace/residence its residents' commutes touch. The metropolitan zoning authority's Lagrangian has no such gap (`χ_i,σ_i,ρ_i,η_i^q` span all of `S` there too), so it and the planner should coincide on the zoning margin exactly (Step 5's investment-neutrality result, unaffected, means investment doesn't separately matter) — the entire welfare gap traces to *fragmentation of scope* (`S^g⊊S`) itself, not strategic behavior. Economically: a municipality that restricts residential land pushes displaced residents elsewhere in the metro, raising congestion/rents wherever they land, and never bears or computes that cost because the absorbing location was never an equation in its own problem — the standard NIMBY/zoning-spillover story, now mechanical rather than asserted. §Municipal Zoning (new remark after `eq:muni-lagrangian`) and §Welfare Comparison's closing `Remark` (third revision, same day) updated in `model.tex`; `social-planner-zoning-derivation.tex`'s closing remark updated to match. |

**Counterfactual framing (relates to item 6, scale of decision):** advisor's July_17th comment frames the counterfactual as two things changing at once — (1) the scale at which the decision is made, (2) the identity of the deciding agent (two agents → one). `model.tex` has a real `\subsection{Social Planner}` right after `\subsection{Scale of the Zoning Decision}` (both after `\subsection{Equilibrium}`), using the same Lagrangian apparatus as §Transportation Authority (`eq:SP-problem`, extending `eq:TA-lagrangian` with a land-identity multiplier `ν_i` and letting `t_i`/`I_kl` be chosen jointly for the whole metro). Investment FOC unchanged in form from `eq:I-optimal` (`eq:I-optimal-SP`). **Full history on the zoning FOC, `zoning-foc-planner`, all 2026-07-23 unless noted:** (2026-07-21) originally added two terms to `zoning-foc-explicit`: (6) cross-jurisdiction spillover, (7) "transit-zoning complementarity." (Correction 1) removed (7) via an envelope-theorem argument ("Why investment does not add a term") — correct as far as it went, but the corrected `zoning-foc-planner` still claimed to equal `zoning-foc-explicit` + term (6), concluding agent identity is welfare-irrelevant. (Correction 2) that didn't survive either: it silently treated the aggregate objective `NΓ(1-1/ε)Φ^{1/ε}` (planner) and the incumbent-style `Σ N_i\bar U_i` objective (municipal/metro-authority, at the time) as interchangeable. Rigorously re-deriving the planner's zoning FOC from its own (aggregate) Lagrangian — same `χ_i,σ_i,ρ_i` construction as item 19's municipal one — showed it has no channel-(1) analogue and uses `Ū` (aggregate) wherever the municipal/metro-authority version used `Ū_i` (local, incumbent); concluded agent identity *does* matter, since the two objectives genuinely differed. (Correction 3, current) prompted by the observation that `Ū` — not `Ū_i` — is the object that's actually equalized across locations at equilibrium (a Fréchet/GEV property, item 18): decided a metro-wide authority should use the same population-wide `Ū` as the planner, since `Ū_i`'s incumbent-protection motivation is specifically a *local* municipal story. With the metropolitan zoning authority's objective corrected to match (item 6), its FOC and the planner's `zoning-foc-planner` coincide exactly — **agent identity is welfare-irrelevant for the zoning margin after all**, now because the objectives were deliberately unified, not assumed to match. The "why investment doesn't add a term" argument was correct throughout all three corrections and extends unchanged to the Nash (not just joint) case. Still doesn't resolve item 1 (road/transit distinction).

**Welfare comparison (2026-07-23, revised same day):** added `\subsection{Welfare Comparison}` to `model.tex`, right after §Social Planner. Proposition~1 (`prop:SP-dominance`) is a pure revealed-preference argument — the DE is feasible for the planner's problem, so `W^SP≥W^DE` globally, no derivatives needed; unaffected by the zoning-FOC correction above. Proposition~2 (`prop:SP-gap`) originally claimed the first-order gap `ΔW=W^SP-W^DE` reduces to `Σ_i[term (6)]_i·(H̄_i^{R,SP}-H̄_i^{R,DE})`, using the *municipal* FOC `zoning-foc` to zero out `∂W/∂H̄_i^R|_DE` — invalid, since that's the first-order condition for the municipal objective `Σ N_i\bar U_i`, not the planner's aggregate `W` used in Proposition 2 (same root cause as the `zoning-foc-planner` correction above). **Revised:** Proposition~2 is now the more modest but actually-provable claim that survives — the investment margin alone contributes zero to first order (the envelope/binding-budget argument, unaffected), so `ΔW≈Σ_i(∂W/∂H̄_i^R)|_DE·(H̄_i^{R,SP}-H̄_i^{R,DE})`. A remark states honestly that this zoning term does *not* reduce further to a clean closed form — it's governed by the same `Λ_i^φ,Λ_i^q,Λ_i^w` system as the planner's own zoning condition, evaluated at DE, deferred to quantification like everywhere else `Λ` objects appear. The *quantitative* counterfactual remains future work, per §Quantification in `comments from SB.md`.

**Next steps (advisor's July_17th comment, logged not actioned):** (1) fully characterize the equilibrium and solve the optimal policy problem; (2) revisit the data plan — after the equilibrium is characterized, decide which parameters need calibration/validation vs. which can be borrowed from existing papers.

## File Map

| File | Role |
|------|------|
| `main.tex` / `main.pdf` | Full proposal: intro, lit review, model, empirics, conclusion |
| `model.tex` / `model.pdf` | Standalone model document — primary development sandbox |
| `two-period-model.md` |Abolished|
| `comments from SB.md` | cohort feedback — **read before any model-section edit** |
| `motivation_draft.md` | Motivation section draft |
| `reference.bib` | BibLaTeX bibliography |
| `main_original.tex` | Snapshot before major revisions (do not edit) |
| `templates/model-section.tex` | Template for new model sections |
| `templates/proposition.tex` | Template for propositions and proofs |
|`zoning_and_transportation_governance_us.md`| informations about how the zoning regulations and transportation investment are decided in the context of United States|
---
|`july_1st_comment.md`|Advisor feedback — **read before any model-section edit** |
## Workflow Rules

**Plan-first:** All non-trivial tasks require an approved plan before implementation. "Trivial" = single-line fix, lookup, or rename.

**Contractor mode:** After plan approval, execute autonomously. Return to user only on genuine ambiguity, a decision requiring user judgment, or a blocker. Do not narrate progress unless something goes wrong.

**First sessions:** Check in slightly more often until the user signals the workflow is calibrated.

**Never change model assumptions or notation** without explicit user approval in a plan. If a model change is needed mid-task, surface it as a checkpoint, not a done deal.

**Commits:** Commit autonomously after each coherent unit of work. Descriptive messages, one per logical unit. User monitors via `git log`. Use `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`.

**Advisor feedback:** Before editing any section of model.tex or main.tex's model section, read `comments from SB.md` `july_1st_comment.md` and update the Open Model Questions table above if relevant.

---

## Quality Standards

**LaTeX:**
- All documents must compile cleanly: zero errors, no overfull hboxes in final output.
- Compile sequence: `latexmk -pdf -biber <file>` (handles pdflatex → biber → pdflatex × 2 automatically).
- Use `align` for multi-line equations; number only equations that are later referenced.
- Use `\text{}` for non-italic text inside math mode.
- Theorems/propositions/lemmas: use `amsthm` environments consistently (see `templates/proposition.tex`).
- Bibliography: `biblatex` + `biber` backend (declared in preamble). Never mix with `natbib` or plain `bibtex`.

**Figures:**
- Publication-ready always: proper axis labels, no inline titles (use captions in LaTeX), font sizes consistent with document body (10–11pt).
- Prefer vector formats: PDF output from TikZ/pgfplots, or PDF/EPS exports from external tools.
- No visual clutter: remove chartjunk, gridlines only if needed, minimal color palette.

---

## LaTeX Environment Setup

The project uses a standard TeX Live installation. Compile from the project root:

```bash
latexmk -pdf -interaction=nonstopmode model.tex      # standalone model
latexmk -pdf -interaction=nonstopmode main.tex       # full proposal
```

Note: `-biber` is not a valid latexmk CLI flag. Biber is invoked automatically when biblatex declares `backend=biber` in the preamble.

Custom commands available in Claude Code:
- `/compile` — compile a LaTeX file and report errors
- `/advisor-checklist` — audit model against SB's open feedback items
