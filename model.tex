\documentclass[12pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\geometry{left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm}
\usepackage{tikz}
\usetikzlibrary{decorations.pathreplacing}
\usepackage{graphicx}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{setspace}
\usepackage{hyperref}
\usepackage[
    bibstyle=numeric,
    citestyle=authoryear,
    backend=biber,
    natbib=true,
    sorting=nyt]{biblatex}
\usepackage{booktabs}
\usepackage{pgfgantt}
\usepackage{titlesec}
\usepackage{indentfirst}
\addbibresource{reference.bib}

\theoremstyle{definition}
\newtheorem{definition}{Definition}

\title{Model}
\author{}
\date{}

\begin{document}
\maketitle

\section{Model}

\subsection{Environment}

Consider a metropolitan area consisting of a finite set of locations $S = \{1, 2, \ldots, n\}$. These locations are partitioned into municipalities $g \in G$:
\[
S = \bigcup_{g \in G} S^g, \qquad S^g \cap S^{g'} = \emptyset \quad \text{for } g \neq g'.
\]

Each location $i \in S$ is endowed with a fixed supply of land $\bar{H}_i$, allocated between residential and commercial use by local zoning:
\[
\bar{H}_i^R + \bar{H}_i^F = \bar{H}_i,
\]
where $\bar{H}_i^R$ is residential floor space capacity and $\bar{H}_i^F$ is commercial floor space capacity. Both are chosen by the municipal government and assumed to be fully utilized in equilibrium.

\subsubsection{Transportation}
Locations are connected by a transportation network $(S, E)$, where $E \subseteq S \times S$ is a set of directed edges (links). Transportation investment $I_{k\ell}$ is chosen at the \emph{edge} level, for each $(k,\ell) \in E$ The utility cost of traveling through edge $(k,\ell)$ is
\[
d_{k\ell} = exp(\kappa \times time_{k\ell}),
\]
where $time_{k\ell}$ is the travel time on edge $(k,\ell)$ and $\kappa > 0$ is a scale parameter, following \cite{Ahlfeldt2015-zb}. The travel time $time_{k\ell}$ is a function of the edge-level traffic $n_{k\ell}$ and investment $I_{k\ell}$:
\[
time_{k\ell} = \delta_{kl}^{0}+\delta_{kl}^{1}\frac{n_{k\ell}^{\rho}}{I_{k\ell}^{\beta}}.
\]
Following \cite{Fajgelbaum2020-yv}, $\delta_{kl}^{0}$ is the free-flow travel time on edge $(k,\ell)$, $\delta_{kl}^{1} > 0$ is a scale parameter, and $\rho, \beta > 0$ are elasticities of travel time with respect to traffic and investment respectively. 
Therefore for a given route r, the commuting cost $\tau_{ij}$ between residence $i$ and workplace $j$ is the product of the costs of all edges along that route:    
\[
\tau_{ijr} = \prod_{(k,\ell) \in r_{ij}} d_{k\ell}(I_{k\ell}, n_{k\ell}),
\]
where $r_{ij}$ is the route chosen by commuters from $i$ to $j$. 

There are three types of agents: households, competitive firms, and governments. Households choose residence and workplace locations. Firms produce a freely tradable good using labor and commercial floor space. Government decisions operate at two levels: municipal governments choose local zoning, and a metropolitan transportation authority chooses investment across all links. In the decentralized equilibrium, municipalities and the transportation authority choose policies simultaneously, each treating the other's decisions as given.

\subsection{Households}

A household chooses a residence location $i$, a workplace $j$, and a route $r$ from a finite, exogenously given choice set $R_{ij}$ of feasible routes between $i$ and $j$ (e.g. the $k$ shortest GIS paths, or distinct road/transit alternatives) to maximize utility. Preferences over the numaire good $Q_{ij}$ and residential floor space $h_{ij}^R$ are Cobb-Douglas:
\[
u_{ijr}(\omega) = \frac{B_i}{\tau_{ijr}}
\left(\frac{Q_{ij}}{\alpha}\right)^{\!\alpha}
\left(\frac{h_{ij}^R}{1-\alpha}\right)^{\!1-\alpha}
v_{ij,r}(\omega),
\]
where $B_i > 0$ is a residential amenity at location $i$, $\alpha \in (0,1)$ is the expenditure share on the composite good, $\tau_{ijr}$ is the commuting cost along route $r$, and $\{v_{ij,r}(\omega)\}$ is a vector of idiosyncratic shocks, jointly distributed according to a nested (generalized extreme value) Fréchet copula with cumulative distribution function
\begin{align}
    \label{GEV}
    F(\{v_{ij,r}\}) = \exp\!\left[-\sum_{ij \in S^{2}} T_iE_j\left(\sum_{r\in R_{ij}}v_{ij,r}^{-\theta}\right)^{\!\epsilon/\theta}\right], \qquad \theta \geq \epsilon > 1.
\end{align}
The outer exponent is $+\epsilon/\theta$, not $-\epsilon/\theta$: a valid CDF must satisfy $F\to1$ as any $v_{ij,r}\to\infty$ holding the rest fixed, which pins the sign down. $T_i,E_j$ enter at the outer $(i,j)$ nest only, not inside the inner route sum: they are location-pair accessibility scales, not route-specific ones, exactly as in the single-route model. Setting $R_{ij}=\{r\}$ for every $(i,j)$ collapses \eqref{GEV} to the original univariate Fréchet marginal $\Pr(v_{ij}\leq z)=\exp(-T_iE_jz^{-\epsilon})$, confirming the nesting strictly generalizes it; $\theta\geq\epsilon$ is required for \eqref{GEV} to be a valid (positively correlated within-nest) GEV copula.

The household's budget constraint is
\[
Q_{ij} + q_i^R h_{ij}^R \leq w_j-\phi(N_i, L_i),
\]
where $q_i^R$ is the residential rent at location $i$, $w_j$ is the wage at workplace $j$. Let $\tilde{W}_{ij} = w_j-\phi(N_i, L_i)$ denote the household's income, where $\phi(N_{i},L_{i})$ is the utility cost from local congestion. Optimal demands are $Q_{ij}^* = \alpha \tilde{W}_{ij}$ and $h_{ij}^{R*} = (1-\alpha)\tilde{W}_{ij}/q_i^R$, giving the deterministic component of indirect utility for a given route,
\[
\tilde{V}_{ijr} = \frac{B_i\, \tilde W_{ij}}{\tau_{ijr}\,(q_i^R)^{1-\alpha}}.
\]

\paragraph{Upper nest choice}: applying the GEV choice-probability formula to \eqref{GEV}, households compare residence-workplace pairs using the route-choice-corrected value $\tilde V_{ij}$, with $T_i,E_j$ entering exactly as in the single-route model:
\begin{align}
    \label{eq:pi}
    \pi_{ij} = \frac{T_i E_j \tilde{V}_{ij}^{\epsilon}}{\sum_{i'j' \in S^{2}} T_{i'} E_{j'} \tilde{V}_{i'j'}^{\epsilon}} = \frac{T_iE_j\tilde V_{ij}^\epsilon}{\Phi},
\end{align}
where $\tilde{V}_{ij}=\dfrac{B_{i}\tilde{W}_{ij}}{\tau_{ij}(q_{i}^R)^{1-\alpha}}$ and $\tau_{ij} = \left(\sum_{r\in R_{ij}}\tau_{ijr}^{-\theta}\right)^{-1/\theta}$ is the Fréchet mean of route costs.such that the population choosing i,j pair is:
\begin{align}
    \label{eq:pi-pop}
    N_{ij} =T_iE_j\tilde V_{ij}^\epsilon \frac{N}{\Phi},
\end{align}

\paragraph{Lower nest choice}: conditional on choosing $(i,j)$, the household chooses a route $r \in R_{ij}$. Following \citet{Allen2022-ms}, we can rewrite $\tau_{ij} = \left(\sum_{r\in R_{ij}}\tau_{ijr}^{-\theta}\right)^{-1/\theta}$ such that
\begin{align}
    \label{eq:pi-route}
    \tau=((I-A)^{-1})^{-1/\theta},
\end{align}
where $A$ is a matrix with elements (k,l) is $d_{kl}^{-\theta}$,such that for matrix $\tau$, the element (i,j) is $\tau_{ij}$.
Accordingly, we can derive the link intensity as follows:
\begin{align}
    \label{eq:edge-traffic}
    \pi_{jk}^{k\ell} = (\frac{\tau_{ij}}{\tau_{ik}d_{kl}\tau_{lj}})^{\theta}
\end{align}
Therefore I can derive the traffic on edge (k,l) as follows:   
\begin{align}
    \label{eq:edge-traffic}
    n_{k\ell} = \sum_{ij \in S^{2}}N_{ij}\pi_{jk}^{k\ell}
\end{align}

\subsection{Firms}

At each location $j$, competitive firms produce a freely tradable good using labor $L_j$ and commercial floor space $H_j^F$:
\[
Y_j = A_j L_j^{\gamma}(H_j^F)^{1-\gamma},
\]
where $A_j > 0$ is location-specific productivity and $\gamma \in (0,1)$. Taking wages $w_j$ and commercial rents $q_j^F$ as given, profit maximization yields:
\[
L_{j}=(\frac{\gamma A_{j}}{w_{j}})^{1/(1-\gamma)}H_{j}^F, \qquad
q_{j}^F=(1-\gamma)(\frac{\gamma}{w_{j}})^{\gamma/(1-\gamma)}A_{j}^{1/(1-\gamma)}.
\]

\subsection{Municipal Zoning}

Land at each location competes for residential and commercial use. Each municipality $g \in G$ chooses an iceberg cost $\{t_i^F\}_{i \in S^g} \geq 0$ on commercial land use at each location: procedural burden — discretionary review, permitting delay, litigation risk, compliance — melts away a fraction of commercial floor space's usable value before it reaches the market. Because the same underlying land parcel can be put to either use, competitive landowners allocate it to whichever use pays the higher effective return; if both uses are active in equilibrium, this competition equalizes returns at the margin:
\begin{align}
    \label{eq:arbitrage}
    q_i^R = \frac{q_i^F}{1+t_i^F}.
\end{align}

Combined with euqation \ref{eq:rent-clearing}, we can get
\begin{align}
    \label{eq:arbitrage1}
    (1-\gamma)(\frac{\gamma}{w_{i}})^{\gamma/(1-\gamma)}A_{i}^{1/(1-\gamma)} = (1+t_i^F)\frac{(1-\alpha)\sum_j N\pi_{ij}\,\tilde{W}_{ij}}{\bar{H}_i^R}.
\end{align}

The municipality sets $\{t_i^F\}$ to maximize the total welfare of its residents. In line with the political economy of local land use \citep{Mast2024-zi, Rollet2025-mk}, the municipal government, acting as a representative of its residents, trades off (1) lower rents and reduced worker-congestion from shrinking commercial land — both favor a higher iceberg cost — against (2) the residential crowding that comes with the resulting in-migration, and (3) the loss of local wage income to residents who both live and work at $i$, both of which favor a lower iceberg cost.
\[
\max_{\{t_i^F\}_{i \in S^g}}\; W^g = \sum_{i \in S^g} N_i\,\bar{U}_i
\quad\text{s.t.}\quad
\eqref{eq:arbitrage},\ \ \bar{H}_i^R + \bar{H}_i^F = \bar{H}_i,
\]
where $N_i$ is the equilibrium population at location $i$ and $\bar{U}_i = \Gamma\!\left(1-\frac{1}{\epsilon}\right)\Phi_i^{1/\epsilon}$ is the expected utility of a representative resident at location $i$, taking other municipalities' iceberg costs $\{t_{i'}^F\}_{i' \notin S^g}$ and transportation investment $\{I_{k\ell}\}$ as given. $\phi(N_i, L_i)$ is a disutility from residing at $i$, capturing two sources of local negative externalities: residential congestion from neighbors and the presence of workers near the residential location:
\[
\frac{\partial \phi}{\partial N_i} > 0, \qquad \frac{\partial \phi}{\partial L_i} > 0,
\]
where $N_i$ denotes residential population and $L_i$ denotes employment at location $i$ (defined above). I parametrize the disutility in Cobb-Douglas form:
\begin{align}
    \phi(N_i, L_i) = \eta\, N_i^{a}\, L_i^{b}, \qquad \eta > 0,\ a, b > 0,
\end{align}
Since $\bar H_i^F = \bar H_i - \bar H_i^R$, the chain rule reduces the municipal first-order condition to a single comparison,
\begin{align}
    \label{zoning-foc}
\frac{dW^g}{dt_i^F} &= \left(\frac{\partial W^g}{\partial \bar{H}_i^R} - \frac{\partial W^g}{\partial \bar{H}_i^F}\right)\\
&\quad\times\frac{d\bar H_i^R}{dt_i^F} = 0,
\end{align}
so the optimum reduces to signing $\partial W^g/\partial \bar H_i^R - \partial W^g/\partial \bar H_i^F$, given $d\bar H_i^R/dt_i^F \neq 0$ (\eqref{zoning-foc-tax} below).

\paragraph{Channels.} Only the $i$-th term of $W^g=\sum_{i'\in S^g}N_{i'}\bar U_{i'}$ depends on $\bar H_i^R,\bar H_i^F$ to leading order, holding other locations' land use and the metro-wide index $\Phi$ fixed. Three exact partials of $\bar U_i=\Gamma(1-1/\epsilon)\Phi_i^{1/\epsilon}$ follow directly from $\Phi_i=\sum_j T_iE_jB_i^\epsilon(w_j-\phi_i)^\epsilon\tau_{ij}^{-\epsilon}(q_i^R)^{-\epsilon(1-\alpha)}$:
\[
\frac{\partial \bar U_i}{\partial q_i^R} = -\frac{(1-\alpha)\bar U_i}{q_i^R}, \qquad
\frac{\partial \bar U_i}{\partial \phi_i} < 0, \qquad
\frac{\partial \bar U_i}{\partial w_i} = \frac{\pi_{ii|i}\,\bar U_i}{w_i-\phi_i} > 0,
\]
where $\pi_{ii|i}$ is the conditional probability that a resident of $i$ also works at $i$ (the local live-work share): the first equality is exact since $q_i^R$ enters $\Phi_i$ as a common factor across every $j$; the third holds because only the $j=i$ term of $\Phi_i$ depends on $w_i$, so the wage channel is scaled down by how many residents actually capture it.

Substituting these, together with $\phi_i=\eta N_i^aL_i^b$, into $\partial(N_i\bar U_i)/\partial \bar H_i^R - \partial(N_i\bar U_i)/\partial \bar H_i^F$ gives the explicit optimality condition
\begin{align}
    \label{zoning-foc-explicit}
&\underbrace{\bar U_i\,\frac{\partial N_i}{\partial \bar{H}_i^R}}_{\text{(1) in-mover}\;\geq\,0}
+\underbrace{N_i\frac{\partial \bar U_i}{\partial q_i^R}\frac{\partial q_i^R}{\partial \bar{H}_i^R}}_{\text{(2) rent}\;\geq\,0}
+\underbrace{N_i\frac{\partial \bar U_i}{\partial \phi_i}\,\eta a N_i^{a-1}L_i^b\,\frac{\partial N_i}{\partial \bar{H}_i^R}}_{\text{(3) residential congestion}\;\leq\,0} \nonumber\\
&-\underbrace{N_i\frac{\partial \bar U_i}{\partial \phi_i}\,\eta b N_i^aL_i^{b-1}\,\frac{\partial L_i}{\partial \bar{H}_i^F}}_{\text{(4) worker-congestion relief}\;\geq\,0}
-\underbrace{N_i\frac{\partial \bar U_i}{\partial w_i}\frac{\partial w_i}{\partial \bar{H}_i^F}}_{\text{(5) local-wage loss}\;\leq\,0}
= 0.
\end{align}
Terms (1)–(5) reproduce the  trade-off: attracting residents and lowering rents (1)–(2), against the residential crowding those in-movers bring (3), net of the worker-congestion relief from shrinking commercial land (4), shrinking $\bar H_i^F$ contracts local labor demand along an upward-sloping labor supply curve — household workplace choice responds to $w_i$ with finite elasticity $\epsilon$ (5). The municipality thus weighs the value of local jobs to its own live-work residents against the congestion those same jobs create.Given the monoticity of $H_{i}^{R}(t_{i}^F)$, the municipal first-order condition \eqref{zoning-foc} on $t_{i}^{F}$ is equivalent to the first-order condition on $\bar H_i^R$.

For now the total land supply is fixed and fully utilized. In a future extension, I will incorporate a endogenous land supply where developers choose whether or not to build.



\subsection{Transportation Authority}

The metropolitan transportation authority chooses investment $\{I_{k\ell}\}_{(k,\ell) \in E}$ to maximize aggregate household welfare, taking the full zoning vector $\{\bar{H}_i^R, \bar{H}_i^F\}$ and the route assignment $\{r_{ij}\}$ as given:
\[
\max_{\{I_{k\ell}\}}\; N \cdot \Gamma\!\left(1 - \tfrac{1}{\epsilon}\right)\Phi^{1/\epsilon}
\]
subject to the budget constraint
\[
\sum_{(k,\ell) \in E} \delta_{k\ell}\, I_{k\ell} \leq K, \qquad I_{k\ell} \geq 0,
\]
where $\delta_{k\ell}$ is the per-unit cost of investment on edge $(k,\ell)$ and $K$ is the total infrastructure budget.

Let $\mu \geq 0$ be the Lagrange multiplier on the budget constraint. Because $\ln \tau_{ij} = \sum_{(k,\ell) \in r_{ij}} \ln d_{k\ell}$, investment on edge $(k,\ell)$ only affects the commuting cost of a residence-workplace pair $(i,j)$ whose route uses that edge, through $\partial \ln \tau_{ij}/\partial I_{k\ell} = -\gamma/I_{k\ell}$ for $(k,\ell) \in r_{ij}$ (and $0$ otherwise). Aggregating over all such pairs, the interior first-order condition for edge $(k,\ell)$ is
\begin{align}
    \label{transit-foc}
\frac{N\gamma}{I_{k\ell}}\,\Gamma\!\left(1-\tfrac{1}{\epsilon}\right)\Phi^{1/\epsilon-1}
\sum_{(i,j)\,:\,(k,\ell) \in r_{ij}} T_i E_j\, \tilde{V}_{ij}^{\epsilon}
= \mu\,\delta_{k\ell}.
\end{align}
The left-hand side is the marginal social benefit of investment on edge $(k,\ell)$: lower travel cost on that edge raises $\tilde{V}_{ij}$, and hence $\Phi$, for \emph{every} residence-workplace pair routed through it — not only pairs directly adjacent to $k$ and $\ell$. This is the sense in which investment spillovers arise endogenously from the network structure: an edge shared by many routes is more valuable to the authority than one used only by its own endpoints, holding the direct commuting-cost elasticity fixed. The authority equates this marginal benefit per dollar of investment across all active edges.

\subsection{Equilibrium}

\begin{definition}[Decentralized Equilibrium]
A decentralized equilibrium is a profile
\[
\Bigl(
\{I_{k\ell}^*\}_{(k,\ell) \in E},\;
\{q_i^{R*},\,q_i^{F*},\,w_j^*\}_{i,j \in S}\Bigr)
\]
such that:
\begin{enumerate}
    \item \textbf{Household optimality.} Commuting flows $\{N\pi_{ij}^*\}$ are given by the Fréchet probability formula at prices $(q_i^{R*}, w_j^*)$ and investment $I^*$, with route-based commuting costs $\{\tau_{ij}^*\}$ and edge traffic $\{n_{k\ell}^*\}$ satisfying \eqref{eq:edge-traffic}.
    \item \textbf{Firm optimality.} Wages $w_j^*$ and commercial rents $q_j^{F*}$ satisfy the firm first-order conditions at $H_j^F = \bar{H}_j^{F*}$ and $L_j^* = N\sum_i \pi_{ij}^*$.
    \item \textbf{Market clearing.} Residential floor space clears at $q_i^{R*}$ given by \eqref{eq:rent-clearing}, commercial floor space is fixed at $H_j^F = \bar{H}_j^{F*}$, and the labor market clears at $L_j^* = N\sum_i \pi_{ij}^*$.
    \item \textbf{Municipal Nash equilibrium.} Each municipality $g$ sets the iceberg cost $\{t_i^{F*}\}_{i \in S^g}$ — equivalently, induces $\{\bar{H}_i^{R*}, \bar{H}_i^{F*}\}_{i \in S^g}$ via \eqref{zoning-foc-tax} — to satisfy the first-order condition \eqref{zoning-foc}, given $\{\bar{H}_{i'}^{R*}, \bar{H}_{i'}^{F*}\}_{i' \notin S^g}$ and $I^*$; no municipality has a profitable deviation.
    \item \textbf{Transportation authority optimality.} $\{I_{k\ell}^*\}$ satisfies the first-order condition \eqref{transit-foc} for every edge $(k,\ell) \in E$, maximizing aggregate welfare given $\{\bar{H}_i^{R*}, \bar{H}_i^{F*}\}$ subject to the budget constraint.
\end{enumerate}
Conditions (iv) and (v) jointly constitute the simultaneous Nash equilibrium between municipalities and the transportation authority: each set of agents takes the other's choices as given and best-responds. The equilibrium iceberg cost $t_i^{F*} \equiv q_i^{F*}/q_i^{R*} - 1$ is computed from the equilibrium quantities in this profile, not solved for as a separate object.
\end{definition}

% \subsection{Quantification}

% The household block delivers a log-linear estimating system once the congestion term is handled: writing $w_j-\phi_i=w_j(1-\phi_i/w_j)$ and taking a first-order log approximation, $\ln(w_j-\phi_i)\approx \ln w_j - \phi_i/w_j$. Substituting into $\ln\pi_{ij}=\ln T_i+\ln E_j+\epsilon\ln\tilde V_{ij}-\ln\Phi$ with $\tau_{ij}=\prod_{(k,\ell)\in r_{ij}} \delta_{k\ell}^\tau n_{k\ell}^\rho/I_{k\ell}^\gamma$ gives a structural-gravity regression on observed commuting flows $N\pi_{ij}$:
% \begin{align}
%     \label{eq:gravity}
% \ln\pi_{ij} \approx\; & \underbrace{\bigl[\ln T_i+\epsilon\ln B_i-\epsilon(1-\alpha)\ln q_i^R\bigr]}_{\text{origin FE}} + \underbrace{\bigl[\ln E_j+\epsilon\ln w_j\bigr]}_{\text{destination FE}} -\epsilon\frac{\phi_i}{w_j} \nonumber\\
% & -\epsilon\!\!\sum_{(k,\ell)\in r_{ij}}\!\!\ln\delta_{k\ell}^\tau -\epsilon\rho\!\!\sum_{(k,\ell)\in r_{ij}}\!\!\ln n_{k\ell}+\epsilon\gamma\!\!\sum_{(k,\ell)\in r_{ij}}\!\!\ln I_{k\ell}-\ln\Phi,
% \end{align}
% in the spirit of \citet{Ahlfeldt2015-zb}. The regressors $\ln n_{ij}, \ln I_{ij}$ of the OD-level friction become route-level sums $\sum_{(k,\ell)\in r_{ij}}\ln n_{k\ell}$, $\sum_{(k,\ell)\in r_{ij}}\ln I_{k\ell}$ once routes span multiple edges — a measurement change (route-level GIS data replace a single OD-level number) rather than an identification problem, since the underlying elasticities $\rho,\gamma$ are still edge-level primitives. With origin and destination fixed effects, the coefficients on these route sums and on $\phi_i/w_j$ identify $-\epsilon\rho$, $\epsilon\gamma$, and $-\epsilon$ respectively. Given $\epsilon$, the origin fixed effect nets out $\ln T_i+\epsilon\ln B_i$ as a residual (observed $q_i^R,\alpha$), the standard model-inversion step.

% The zoning iceberg cost requires no approximation: \eqref{eq:arbitrage} gives $t_i^F= q_i^F/q_i^R-1$ directly from residential and commercial rent data, with no estimation. Given a constructed $\hat\phi_i$ from the step above, the congestion parameters follow from
% \begin{align}
% \ln\hat\phi_i = \ln\eta + a\ln N_i + b\ln L_i + u_i,
% \end{align}
% recovering $(\eta,a,b)$ directly, with $a>1$ testable against the convexity restriction.

% The comparative statics inside \eqref{zoning-foc-explicit} ($\partial N_i/\partial \bar H_i^R$ etc.) are a general-equilibrium object: $N_i, q_i^R, w_i, \phi_i$ are jointly determined through $\Phi=\sum_{i'}\Phi_{i'}$ and the labor market, so their response to a zoning change is not a single-equation partial derivative — exactly why counterfactuals in this literature are usually solved as a linearized system rather than a closed-form elasticity. A tractable special case treats each location as small relative to the metro ($d\ln\Phi\approx0$, wages elsewhere unaffected), collapsing the metro-wide fixed point to a local system in $(\hat N_i,\hat L_i,\hat q_i^R,\hat w_i,\hat\phi_i)$ solvable by matrix inversion; working this out explicitly, including the wage channel in \eqref{zoning-foc-explicit}, is the natural next step.

\subsection{Data and Calibration}

Following \citet{Fajgelbaum2020-yv}'s treatment of network and cost fundamentals, parameters split into three groups: estimated from the model's own equations (\S Quantification), set from external estimates, and location/link fundamentals recovered as residuals or calibrated directly to observed infrastructure costs.

\paragraph{Data.} Commuting flows $N\pi_{ij}$ and employment $L_i$: LODES/LEHD origin-destination data. Wages $w_j$ and population $N_i$: ACS. Residential and commercial rents $q_i^R, q_i^F$: county appraisal district records or commercial listing data (CoStar), jointly identifying $t_i^F$ via \eqref{eq:arbitrage}. Land area $\bar H_i$: parcel/GIS data. Network edges $E$, route assignment $\{r_{ij}\}$, and travel times, for $\delta_{k\ell}^\tau$: GIS road network shortest paths and observed transit line/transfer sequences. Historical investment $I_{k\ell}$ and per-unit construction costs $\delta_{k\ell}$: NTD and state DOT capital programs, at the edge level. Total budget $K$: observed metro-area transportation capital spending over the relevant period.

\paragraph{Estimated from model equations.} $\epsilon, \rho$, and the commuting-cost investment elasticity in \eqref{eq:gravity}; $(\eta,a,b)$ from the congestion regression; $t_i^F$ is not estimated at all, but read directly off rent data via \eqref{eq:arbitrage}.

\paragraph{Set externally.} $\alpha$, the consumption expenditure share, from the CEX or national accounts; $\gamma$, the firm's labor share in \eqref{eq:rent-clearing}'s underlying production function, from the local labor share of income (BEA). \emph{Notation note:} the commuting-cost investment elasticity in $d_{k\ell}=\delta_{k\ell}^\tau n_{k\ell}^\rho/I_{k\ell}^\gamma$ currently reuses the symbol $\gamma$ already assigned to the firm's labor share in $Y_j=A_jL_j^\gamma(H_j^F)^{1-\gamma}$ — these are distinct parameters with distinct empirical targets and should be given separate symbols before the calibration is finalized.

\paragraph{Fundamentals recovered as residuals.} $T_i, B_i$ (jointly, from the origin fixed effect) and $E_j, A_j$ (from the destination fixed effect and the firm's first-order condition) so that the model exactly matches observed $N_i, L_i, w_j, q_i^R$ at the calibrated parameters, following the model-inversion logic of \citet{Ahlfeldt2015-zb} and \citet{Fajgelbaum2020-yv}.

% \subsection{Social Planner}

% A social planner jointly chooses zoning allocations $\{\bar{H}_i^R, \bar{H}_i^F\}$ and transportation investment $\{I_{k\ell}\}$ to maximize aggregate household welfare:
% \[
% \max_{\{\bar{H}_i^R,\,\bar{H}_i^F\},\,\{I_{k\ell}\}}\;
% N \cdot \Gamma\!\left(1-\tfrac{1}{\epsilon}\right)\Phi^{1/\epsilon}
% \]
% subject to the budget constraint $\sum_{(k,\ell)\in E}\delta_{k\ell}I_{k\ell} \leq K$, land constraints $\bar{H}_i^R + \bar{H}_i^F = \bar{H}_i$ for all $i$, and market clearing.

% Comparing the planner's first-order condition for zoning with the municipal condition in (4) reveals two positive terms that municipalities systematically ignore. First, because each municipality $g$ maximizes welfare only over $i \in S^g$, it ignores the welfare of residents in neighboring municipalities who gain from lower rents and more accessible housing when $g$ rezones — even though those residents move voluntarily and are made better off. Second, municipalities do not account for the transit-zoning complementarity: denser residential and commercial development near transit-accessible locations raises ridership and the marginal return to transit investment, generating a positive externality on the transportation authority's problem. Road investment is comparatively less sensitive to local density, so this externality operates asymmetrically across infrastructure types.

% Both omitted terms are positive, implying that decentralized municipalities choose zoning that is too restrictive on both margins relative to the social optimum:
% \[
% \bar{H}_i^{R,\,\mathrm{SP}} \geq \bar{H}_i^{R,\,\mathrm{DE}}, \qquad
% \bar{H}_i^{F,\,\mathrm{SP}} \geq \bar{H}_i^{F,\,\mathrm{DE}}
% \qquad \text{for all } i \in S.
% \]
% The decentralized equilibrium therefore features underinvestment in transit — not because the transportation authority errs given the zoning it observes, but because the zoning it faces is itself distorted by fragmented local governance.

\printbibliography

\end{document}
